<% if(currentUser) { %>
  <div class="user-card-container">
    <p class="user-greeting" >Welcome, <%= currentUser.name %>!</p>
    <p class="user-role">Account type: <%= currentUser.role %></p>
  </div>
<% } %>