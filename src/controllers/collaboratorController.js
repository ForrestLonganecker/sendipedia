const collaboratorQueries = require('../db/queries.collaborators.js');
// const Authorizer = require('../policies/collaborator');

module.exports = {
  show(req, res, next){
    // will show the collaborators for a given wiki as well as the 
    // form to add/remove them
  }
}